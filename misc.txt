triggerBlock(int x, int y);
triggerArea(int x, int y, int width, int height);

spawnEntity() spawnBullet() spawnExplosionParticle()
spawning respawn

decrement / increment skill / ability
slot, stack

stunned getStunned()

baseItem

class TiledSpritemap

FP.alarm


orage, eclairs, tempete, pluie, vent
son de cloche